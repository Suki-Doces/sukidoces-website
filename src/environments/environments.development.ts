export const environment = {
  production: true,
  apiUrl: 'example-host/suki-doces', // <- Endereço da sua API Node.js hospedada no Render
  productImgUrl: 'example-host/imagens/' // <- URL base para as imagens dos produtos
};
