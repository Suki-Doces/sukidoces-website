export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/suki-doces',
  productImgUrl: 'http://localhost:3000/imagens/'
};

// Este arquivo é gerado pelo script `npm run config-env` usando o src/.env.
// Se ele ainda não existir, o aplicativo usa valores locais para desenvolvimento.

